# Init Files, Variables and Expansions

This directory contains Bash scripts about shell initialization files, variables, expansions, aliases, and shell arithmetic.

## 0-alias

Creates an alias named `ls` whose value is `rm *`.
